# RUNing HUB 后端配置模板
# 用法：复制为 backend/.env 并填入真实值（.env 已加入 .gitignore，不会上传）

# 数据库
RUNHUB_DB_HOST=127.0.0.1
RUNHUB_DB_PORT=3306
RUNHUB_DB_USER=root
RUNHUB_DB_PASSWORD=改成你的数据库密码
RUNHUB_DB_NAME=runhub

# JWT 密钥（生成：python -c "import secrets; print(secrets.token_hex(32))"）
RUNHUB_SECRET_KEY=改成随机字符串
